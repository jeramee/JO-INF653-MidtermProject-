<?php
// Variable dump for types
echo "<h2>Types:</h2>";
var_dump($types);
foreach ($types as $type) {
    // Loop contents
}
echo "<br><br>";

// Variable dump for makes
echo "<h2>Makes:</h2>";
var_dump($makes);
foreach ($makes as $make) {
    // Loop contents
}
echo "<br><br>";

// Variable dump for classes
echo "<h2>Classes:</h2>";
var_dump($classes);
foreach ($classes as $class) {
    // Loop contents
}
echo "<br><br>";

echo "<h2>Vehicle:</h2>";
var_dump($vehicles);
foreach ($vehicles as $vehicle) {
    // Loop contents
}
echo "<br><br>";

echo "<h2>Filtered Vehicle:</h2>";
var_dump($filteredVehicles);
foreach ($filteredVehicles as $vehicle) {
    // Loop contents
}
echo "<br><br>";
?>